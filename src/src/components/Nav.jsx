import { AppBar, Toolbar } from '@mui/material'
import React from 'react'

export default function Nav() {
  return (
    <AppBar>
        <Toolbar>
            test
        </Toolbar>
    </AppBar>
  )
}
